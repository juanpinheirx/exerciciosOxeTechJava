public class squareArea {
    void squareAreaTimes2(int l) {
        System.out.println("A área do quadrado é: " + (l * l) + "\n" + "E a multiplicação por dois desta área é: " + ((l * l) * 2));
    }
}
